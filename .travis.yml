language: php
php:
  - 7.0
  - 8.0
  - nightly

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  allow_failures:
    - php: nightly

before_install:
  - travis_retry composer self-update

install: travis_retry composer install

script: vendor/bin/phpunit